# Use a distinct socket path for webpack-dev-server HMR to avoid colliding with backend WS endpoints
WDS_SOCKET_PATH=/sockjs-node
# Ensure the app's service logs WS connects to backend directly
REACT_APP_WS_URL=ws://localhost:8080
